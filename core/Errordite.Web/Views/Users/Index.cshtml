
@using Errordite.Web.Extensions
@using Errordite.Web.Models.Users
@model UsersViewModel
           
@{
    ViewBag.Title = "Users";
    Layout = "~/Views/Shared/Master2.0.cshtml";
}

@section header
{
    @Html.Partial("Admin/Tabs") 
}

<section id="users" class="centered">
    <h1>Users</h1>
    <p>Your organisation's users are listed below. <a href="@Url.AddUser()">Click here to add a user</a></p>
    @if (Model.Users != null && Model.Users.Any())
    {
        @Html.Partial("Users/List", Model)
    }
    else
    {
        <p>No users were found.</p>
    }
</section>